---
language: python
python: "2.7"
sudo: true

before_install:
  - echo "localhost ansible_connection=local" >> test.yml

install:
  # Install ansible
  - pip install ansible

script:
  # Check the role/playbooks syntax
  - rvm install 2.2.1 && rvm use 2.2.1 && ansible-playbook -i test.yml ansible/site.yml